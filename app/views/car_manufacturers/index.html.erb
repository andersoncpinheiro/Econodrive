<h1>Car Manufacturer</h1>

<table class="table table-striped table-hover table-bordered">
  <thead>
    <tr>
      <th>Manufacturer</th>
      <th>Car Model </th>
      <th>Year </th>
      <th>Capacity</th>
      <th>Type Fuel</th>
      <th> Reported Consumption</th>
    </tr>
  </thead>

  <tbody>
    <% @car_manufacturer.each do |f| %>      
      <tr>
        <td><%=f.manufacturer %></td>
        <td><%=f.car_model %></td>
        <td><%=f.year%></td>
        <td><%=f.capacity%></td>
        <td><%=f.type_fuel_id%></td>
        <td><%=f.reported_consumption%></td>
          <td> 
          <%= link_to t('links.Edit'), edit_car_manufacturer_path(f) %>
          <%= link_to t('links.Destroy'), f, method: :delete, data: { confirm: ('Vocẽ tem certeza?') } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New Car Model', new_car_manufacturer_path, class: 'btn btn-primary' %>